- [project_knowledge] EasyCode 项目入口与启动流程 — 入口文件：src/main/java/com/easycode/Main.java，39行。启动顺序：ConfigLoader.load("easycod...
- [project_knowledge] EasyCode 内置工具与 Tool 接口 — 项目包含6个内置工具，位于 src/main/java/com/easycode/tool/：ReadFileTool、WriteFileTool、Edi...
- [project_knowledge] EasyCode 工具注册机制 — ToolRegistry 使用 LinkedHashMap 存储工具，key 为工具名。启动时在 Main.java 中手动注册6个内置工具。随后 Mcp...
- [project_knowledge] EasyCode MCP 接入架构 — MCP 接入分五层：1) 配置加载：用户级 ~/.easycode/mcp.yaml 和项目级 easycode.yaml 合并；2) 传输层：Stdio...
- [project_knowledge] EasyCode 架构概览 — 项目启动入口 Main.java 顺序加载配置、创建LLM提供者、注册6个内置工具、初始化对话管理、权限、MCP扩展，最后启动Agent循环和TUI。To...
- [project_knowledge] EasyCode 会话管理实现 — 会话管理由 SessionContext、ConversationMgr 和 ReplacementLedger 三个核心类实现。SessionConte...
- [project_knowledge] EasyCode 会话管理实现 — 会话管理由 SessionContext、ConversationMgr 和 ReplacementLedger 三个核心类协作实现。SessionCon...
- [project_knowledge] EasyCode 记忆系统（上下文压缩）实现 — 记忆系统（上下文压缩）采用两层+三条路径架构，由 ContextManager 统一调度。  ## 第一层：Offloader - 规则 F1：单条工具结...
- [project_knowledge] EasyCode 上下文压缩机制 — 压缩采用两层+三条路径架构。第1层Offloader本地落盘工具结果（F1单条>20KB落盘替换为预览，F2单轮聚合>200KB落盘至阈值以下），轻量且始...
- [project_knowledge] EasyCode 会话结束判断机制 — AgentLoop.run() 有8种退出路径：1) 模型返回纯文本（最常见）；2) 仅思考文本；3) 达到最大10轮工具调用；4) 用户取消；5) Pr...
- [correction_feedback] 指令加载系统应考虑去重 — 用户反馈：三层加载（项目根、.easycode/、~/.easycode/）仅用 StringBuilder 拼接，若 LLM 判断边界不清可能在多层中记...
- [project_knowledge] EasyCode Skill 系统概述 — EasyCode 目前内置三个 Skill：/commit（基于 git diff --staged 生成 commit message）、/review...
- [correction_feedback] 代码审查发现 CODEX.md 与 EasyCode.md 内容重复 — 项目根目录存在 CODEX.md 和 EasyCode.md 两个文件内容完全相同（均为 2152 字节），属于内容重复。建议删除其中一个或通过符号链接关...
- [project_knowledge] EasyCode 团队功能（Team Lead 系统） — Team Lead 系统是 ch15 新开发的协作功能，包含以下模块：- Team / TeamMember: 持久化小组对象，记录成员、工作目录、运行后...
- [project_knowledge] EasyCode 团队功能测试命令与覆盖情况 — ## 测试文件 - TeamEndToEndTest（12 个测试）：覆盖 SharedTask/Message 基础、依赖校验、Mailbox 收发、T...
- [correction_feedback] write_file 工具抛出 'path is null' 异常 — 在执行写入 hello.txt 时，write_file 工具抛出异常 "path is null"，导致写入失败，最终通过 exec_command 执...
- [project_knowledge] EasyCode 电商系统模块规划 — 基于 Java Spring Boot 的多模块电商子系统 — 用户要求构建电商系统，已全权授权助理决策。计划在 EasyCode 父 POM 下新增 ecommerce/ 子模块，原有 EasyCode 代码移入 e...
- [project_knowledge] EasyCode 工具分类与权限机制的独立性及过滤规则 — Tool 接口同时定义了 Category 枚举（SEARCH/FILE/SHELL）和 Permission 枚举（READ_ONLY/READ_WRI...
- [project_knowledge] EasyCode 权限管道 (PermissionPipeline) 及执行审批四层模型 — 工具调用执行时通过 PermissionPipeline 进行四层过滤，而非在可见性阶段按权限裁剪。AgentLoop 始终将全部工具列表发给 LLM（仅...
- [project_knowledge] EasyCode 上下文压缩第二层后自动恢复机制 (RecoveryBuilder) — SummaryGenerator 生成全量摘要后，通过 conv.replaceAll() 一次性原子重建整个会话历史，完全自动，无需 LLM 或用户干预...
- [user_preference] 用户名称偏好 - lqs — 用户自称为 lqs，助手应记住并称呼其为 lqs。
- [project_knowledge] EasyCode 持久记忆更新机制 (MemoryUpdater) — MemoryUpdater 负责异步更新跨会话记忆。会话结束后调用 updateAsync()，启动独立线程将近期对话与现有 MEMORY.md 索引发送...
- [project_knowledge] EasyCode 持久记忆更新机制 — 持久记忆更新由 MemoryUpdater 异步驱动，会话结束后 updateAsync() 启动独立线程，将近期对话与现有 MEMORY.md 索引发给...
- [project_knowledge] EasyCode 持久记忆系统（Memory包）实现 — 持久记忆跨会话积累，由 MemoryStore、MemoryUpdater、MemoryInjector 三个核心类实现。  - **存储层 Memory...
- [correction_feedback] 记忆系统缺少自动清理或手动管理命令 — 用户 lqs 询问记忆一直增加如何删除和清理。当前机制仅依赖 MemoryUpdater 中 LLM 判断的 delete 操作或用户手动删除 .easy...
- [project_knowledge] .easycode/memory/MEMORY.md 索引与笔记文件的关系 — MEMORY.md 是笔记文件的快照索引，每行格式 `- [{type}] {title} — {前80字}`。MemoryInjector.build(...
- [project_knowledge] EasyCode 会话持久化机制 — SessionWriter 实时 JSONL 追加写入与 fsync、SessionResumer 在线恢复、SessionCleaner 30天自动清理 — 会话持久化采用 JSONL 格式实时追加写，每条消息通过 SessionWriter 调用 fsync 刷盘，确保停电不丢数据。ConversationM...
- [project_knowledge] EasyCode 内置命令框架与 UI 事件循环集成 — CommandDispatcher.dispatch() 在 Tui 主循环中每次读取用户输入后调用。解析结果如为命令则直接执行，返回 CommandRe...
- [project_knowledge] EasyCode Prompt 结构与加载机制 — ## 三层结构  ### 第一层：指令加载 (InstructionLoader) - 启动时一次加载三层路径：① 项目根/EasyCode.md ② 项...
- [project_knowledge] EasyCode Hook 系统实现 — ## Hook 系统  ### 概述 Hook 是 EasyCode 的事件驱动自动化扩展机制，通过项目根目录的 `easycode.hooks.yaml...
- [project_knowledge] EasyCode Sub Agent 系统 - 内置 reviewer 与自定义 Agent 加载机制 — ## Sub Agent 模式 目前有一个内置 Sub Agent：reviewer（代码审查），使用 defined 模式，可用工具 read_file...
- [correction_feedback] 子 Agent 审查全 src 超时且后台无自动返回问题 — 子 Agent reviewer 在审查整个 src 时出现功能问题：1) 第一轮主 Agent 调用 run_agent 让 reviewer 审查全 ...
- [correction_feedback] 子Agent 代码审查发现的关键问题（RunAgentTool taskId 不匹配等） — 子Agent reviewer 审查全部 13 个 Java 源文件后提交报告，发现严重 Bug 1 个、高优先级问题 4 个、中优先级问题 11 个、低...
- [correction_feedback] 子Agent不能正常返回结果的原因分析（taskId不匹配与超时） — ## 问题现象 用户使用子 Agent（reviewer）审查全项目 13 个 Java 源文件时，子 Agent 虽然执行完成（状态 DONE），但主 ...
- [correction_feedback] 子Agent worktree 隔离模式下工具调用全部取消问题 — 用户使用 Agent 工具派送 general-purpose 子 Agent，isolation 设为 "worktree"，任务为修改 witness...
- [correction_feedback] 子Agent worktree 隔离模式下完成但无文本输出问题 — 用户使用 Agent 工具派送 general-purpose 子 Agent，isolation 设为 "worktree"，任务为修改 witness...
- [correction_feedback] 子Agent worktree 隔离模式下报告成功但 worktree 目录用户找不到 — 用户 lqs 使用 Agent 工具派送 general-purpose 子 Agent，isolation 设为 "worktree"，任务为修改 wi...
- [correction_feedback] 子Agent worktree 隔离模式下修改 untracked 文件会污染主工作区 — 用户 lqs 发现：使用 general-purpose 子 Agent 在 worktree 隔离模式下修改一个原本 untracked 的文件（wit...
- [correction_feedback] 子Agent worktree 隔离模式下达到最大轮次且无有效输出导致 ERROR — 用户 lqs 派送 general-purpose 子 Agent，isolation=worktree，任务修改 witness.txt 内容为 '12...
- [correction_feedback] 子Agent worktree 隔离模式下达到最大轮次且无有效输出导致 ERROR — 用户 lqs 使用 Agent 工具派送 general-purpose 子 Agent，isolation 设为 worktree，任务修改 witne...
- [correction_feedback] 子Agent worktree 任务完成后自动清理机制缺失 — 用户 lqs 在使用 general-purpose 子 Agent、isolation=worktree 执行任务后，询问任务完成后 worktree ...
- [project_knowledge] EasyCode 子 Agent 轮次完成行为 — 子 Agent（如 general-purpose）在 worktree 隔离模式下执行任务时，如果提前完成（即不再需要调用工具），会立即返回 DONE ...